#include"main.h"
/**
 * add - Adds two numbers and return the result
 *
 * @a (int): First integer
 * @a (int): Second integer
 * Return: An integer as a result
 */
int add(int a, int b)
{
	return (a + b);
}
