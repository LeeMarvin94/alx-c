#include"main.h"
/**
 * main - Prints all single digit numbers of the 10 base
 *
 * Return: Always zero
 */
int main(void)
{
	int c;

	for (c = 48; c < 58; c++)
	{
		putchar(c);
	}
	putchar('\n');

	return (0);
}

